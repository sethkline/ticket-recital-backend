'use strict';

/**
 * early-access-phrase service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::early-access-phrase.early-access-phrase');
