'use strict';

/**
 * early-access-phrase router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::early-access-phrase.early-access-phrase');
